# GrowthX — 인사평가 솔루션

## 하네스: 인사평가 솔루션 풀스택 개발

**목표:** 인사평가(HR 성과평가) 솔루션 웹사이트를 와이어프레임부터 Docker 배포까지 5인 전문 에이전트(디자인·프론트·백엔드·QA·릴리스)로 개발한다. **기본 실행 = 필요한 에이전트만 띄우는 최소 범위 서브에이전트**(토큰·속도 절감), 5인 풀팀은 신규 대형 빌드에서만. 모델은 backend만 opus, 나머지 sonnet (상위 모델 일괄 상향 금지 — 속도는 병렬 팬아웃으로). 프론트 작업은 `nextjs-frontend` 스킬 필수.

**트리거:** 인사평가/성과평가/평가 시스템·솔루션 관련 개발 작업(화면 설계, 프론트엔드, 백엔드 API, 통합 QA, 배포)이나 그 부분 재실행·수정·보완 요청 시 `eval-harness-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**제품명:** **에너지엑스 인사 평가** (이전 "GrowthX" 브랜드 폐기 — UI/문서 표기는 모두 "에너지엑스 인사 평가").

**스택:** Next.js(App Router) 프론트 + **Part 하이브리드 디자인 시스템**(루트 `DESIGN.md`가 디자인 SSOT — 2026-07-03 전면 재작성), **기본 글꼴 Pretendard**(한글 최적화) + NestJS/Prisma/PostgreSQL **모듈러 모놀리식** 백엔드 + **OpenAPI 계약우선**(`@nestjs/swagger` 발행 → orval codegen) + Docker 자체 호스팅. **Turborepo + pnpm 모노레포**(`apps/*` 배포 단위 + `packages/*` 공유: ui·contracts·auth·config) — **서비스가 늘어나는 멀티서비스 플랫폼** 전제. 등급·풀·인상률·가중치 등 수치 규칙은 **설정 가능(`RuleSet`)**, 에너지엑스 2026 값을 기본 seed. **E2E 테스트는 루트 `e2e/`**(Playwright Test Agents — 🎭planner→generator→healer, `pnpm test:e2e`; 인증은 localStorage `gx.*` storageState 주입, 앱은 `webServer` 자동기동/`reuseExistingServer`). Playwright 업데이트 시 `npx playwright init-agents --loop=claude` 재생성.

> **아키텍처 SSOT(2026-06-15):** 코드 구조의 권위는 `eval-harness-orchestrator/references/architecture.md`. 모노레포 레이아웃·모듈 경계(미래의 절단선·DB schema 분리)·수직슬라이스·**파일당 ~200줄 상한**·모듈 매니페스트 README를 정의(바이브 코딩 — AI 가독성). 외부 API 소비/제공은 `integration-adapter.md`(부패 방지 어댑터). **하네스 문서·에이전트·스킬은 정렬 완료, 코드 모노레포화는 Phase 2~3에서 점진 적용**(현재 `apps/web`+`apps/api`는 그대로 한 서비스).

> **디자인 권위 변경(최종 2026-07-03):** Toss → Kinetic Enterprise(06-11) → Notion Low-Color(06-21) → **Part 하이브리드**(07-02 클라이언트 확정 시안 `Part/image N.png` 기준, 07-03 `DESIGN.md` 전면 재작성으로 유일 SSOT 승격). 쿨그레이 캔버스 `#F8F9FD` + 흰 카드 **10px 라운드**·옅은 중립 그림자 + 진네이비 텍스트 `#161326` + 액션 블루 `#0257CE` 단일 강조 + **사이드바 전용** 퍼플 `#564599` + 등급 S~D 5색 배지(S `#7C3AED`/A `#0EA05E`/B `#F97316`/C `#F5B400`+진갈색 글씨/D `#EF4444`). **기본 글꼴 Pretendard 유지**. ⚠️ tailwind preset 리맵: `rounded-sm`=6px(라벨)/`rounded-md`=8px(컨트롤)/`rounded-lg`=10px(카드) — tailwind 기본 px로 착각 금지. 이전 체계(Kinetic 퍼플 팔레트·Toss·Notion radius0 웜그레이) 전부 폐기.

**핵심 도메인(권위 자료 PPT·xlsx 확정):** 평가 = **KPI/성과 중심**(연봉·최종등급 산정은 실적만). **역량평가는 폐기 아님 — 참고용 백데이터로 존재**(연 1회 12월·10문항·S/A/B/C/D, 가중치 100점이나 **연봉·등급 미반영**, `CompetencyQuestion`/`CompetencyResponse`로 구현, 조회·연도비교 화면에 표시). 평가 유형 = **본인평가(self) + 다단계 부서장 평가(downward 3단계: round1 팀장·round2 본부장·round3 그룹대표, 단계가중 0.5/0.3/0.2 + 평가자 동일인 예외)** — 수평/상향/다면평가 **없음**. 조직 = **그룹→본부→팀→개인 4단계**(그룹 최상위). 등급 풀 단위 = **그룹**. KPI 2그룹 = 성과중심(매출액·공정액·수주&업무수행 70/80%) + 협업·성장(협업성과·자기개발 20/30%). 등급은 측정방식별(금액 달성률/건수).

**단일 진실 공급원** (`.claude/skills/eval-harness-orchestrator/references/`):
- `domain-model.md` — 엔티티·역할(4)·KPI 분류·평가 유형(self/downward)·조직(그룹→본부→팀)·상태 머신·명명
- `business-rules.md` — 등급·측정방식별 달성률·그룹 풀·KPI 가중치·인상률·캐스케이드·타임라인·RBAC (설정 가능, 2026 seed)
- `architecture.md` — **코드 구조 SSOT**: 모노레포(Turborepo/pnpm)·모듈 경계·수직슬라이스·파일상한·매니페스트·신규 서비스 스캐폴드
- `api-contract-convention.md` — 응답 봉투·경로·인증 + **OpenAPI 발행·orval codegen·버저닝·외부 인증**
- `integration-adapter.md` — 외부 API **소비(부패방지)·제공(키 인증)** 규약
- `reference-ui-screens.md` — 레퍼런스 솔루션 화면 인벤토리·컴포넌트 (참고용)
- 시각 언어 SSOT는 루트 `DESIGN.md`(Kinetic Enterprise). `tds-design-language.md`는 폐기 스텁(historical)

**권위 자료(따라야 할 기준):** `인사 평가 시스템 참고용/2026년도 임직원 KPI 및 평가 운영 계획_VER3 1.pptx`(운영계획 PPT)가 최우선. KPI 양식 xlsx가 평가기준 보완. **레퍼런스 솔루션 이미지는 참고용(advisory)** — 화면 아이디어 참고만, 규칙·도메인·평가차원은 PPT/xlsx 우선(역량/다면평가는 레퍼런스에서 잘못 끌어왔던 것 → 폐기). 직책 체계: 대표이사·본부장·팀장·책임·선임·프로.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-02 | 초기 구성 (에이전트 5 + 스킬 6 + 도메인/계약 레퍼런스) | 전체 | 인사평가 풀스택 개발 하네스 신규 구축 |
| 2026-06-02 | 참고 자료 반영 — 도메인 모델 전면 재작성(에너지엑스 2026 KPI), business-rules·reference-ui-screens·tds-design-language 레퍼런스 추가, 5개 스킬·4개 에이전트 갱신 | 레퍼런스 4종, 스킬·에이전트 | PPT/엑셀/요구사항/레퍼런스 UI/TDS 자료로 하네스 확고화 |
| 2026-06-02 | 권위 자료 = PPT 운영계획으로 명시(레퍼런스 이미지는 참고용 격하), 직책 체계 정정(대표이사·본부장·팀장·책임·선임·프로), 레퍼런스 화면 S9·S10 + 역량 레이더/산점도 컴포넌트 인벤토리 추가 | domain-model, business-rules, reference-ui-screens, orchestrator, 디자인 스킬·에이전트 | 사용자 피드백: PPT 우선·실제 직책 체계 반영 |
| 2026-06-02 | 1차 빌드(M1) 실행 — 모노레포 apps/api(NestJS·Prisma 20엔티티·규칙엔진)·apps/web(Next.js)·Docker. QA 조건부 통과 후 결함 수정 | apps/*, _workspace/* | "실제 개발 착수" |
| 2026-06-02 | **도메인 대정정(PPT·xlsx 재확인)** — ①역량평가 폐기(권위 자료에 없음, 레퍼런스서 오인) ②다면평가(수평/상향) 폐기 → self+downward(1·2차)만 ③조직 그룹→본부→팀→개인 4단계(그룹 최상위) ④등급 풀=그룹 단위 ⑤KPI 2그룹·측정방식별 등급(xlsx) | domain-model, business-rules, 전 스킬·에이전트, 와이어프레임, apps/* 재빌드 | 사용자 피드백: 다면평가 없음·그룹 최상위·xlsx 기준 평가기준 |
| 2026-06-02 | 빌드·배포 **실검증 완료**(Node 설치) — TS 빌드 에러 7건·Prisma OpenSSL binaryTargets·entrypoint dist 경로·seed ts-node·web healthcheck IPv6·API 프록시(API_PROXY_TARGET) 등 결함 7건 수정. Docker 스택 healthy·데모 로그인 통과. GitHub(hajun13/Growthx) 푸시 | apps/*, docker-compose, Dockerfile, .env | 사용자: "실제 개발 착수·빌드 검증·깃 업로드" |
| 2026-06-02 | **디자인 전면 재스키 (TDS→Apple)** + 브랜드 변경(GrowthX→에너지엑스 인사 평가) + UX 사용자 친화 개선. `DESIGN.md`(Apple, getdesign) 디자인 SSOT화, 글꼴 Pretendard 유지. apps/web 토큰(tailwind·globals)·공용 컴포넌트·셸/네비·로그인 재작업 | DESIGN.md, CLAUDE.md, apps/web | 사용자 피드백: "이 디자인 패턴(Apple)·Pretendard·UX 개선·브랜드 에너지엑스 인사 평가" |
| 2026-06-05 | **연도 누적(YoY) 비교 시스템** — 작년(2025) KPI 결과 DB 적재 + 연도 비교. ①스키마 보강(User.legalEntity·employmentStatus·resignedAt, EvaluationResult 조직 스냅샷) ②2025 closed 사이클·전용 RuleSet ③과거결과 임포트(평가자정리 파서·이름매칭 재직/퇴사/검토큐, 88행 적재 검증 S3·A48·B25·C8·D4 원본 일치) ④비교 API(/results/compare·/distribution) ⑤화면 `/reports/yoy`(개인 타임라인·조직 등급분포·규칙차이 배너·법인필터) ⑥QA(byType 두 shape live/import 정합). **역량평가 정정: 폐기 아님 → 참고용(연봉 미반영)** | schema.prisma, excel/results 모듈, apps/web `/reports/yoy`, CLAUDE.md | 사용자: "작년 결과 DB 적재·연도 누적 비교·미래환경플랜·퇴사자 처리" |
| 2026-06-08 | **하네스 효율화 (속도·토큰·프론트 스킬 강제)** — ①기본 실행을 5인 풀팀→**최소 범위 서브에이전트**로 전환(풀팀은 신규 대형 빌드 전용), `SendMessage` 조율 오버헤드 제거 ②**모델 차등**: backend만 opus(점수·규칙·계약), 나머지(designer·frontend·qa·release) sonnet ③**frontend-engineer는 `nextjs-frontend` 스킬 필수 선호출**(우회 구멍 "또는 그 절차를 따른다" 제거) ④각 에이전트 필요한 레퍼런스만 Read(컨텍스트 절약) | orchestrator SKILL.md, agents/* 4종, CLAUDE.md | 사용자 피드백: "시간 너무 오래 걸림·토큰 과다·프론트는 무조건 nextjs-frontend 스킬" |
| 2026-06-08 | **중간 점검 화면 재구성 (루틴 가시화·디자인 정렬·아이콘)** — 백엔드/계약 불변, 프론트만(designer→frontend→qa 서브에이전트). ①`MidtermStepper` 신규 — 역할별 단계(구성원 5: KPI확인→자가점검→피드백확인→보완조치→재조정 / 부서장 4: 진척검토→확인·피드백→보완조치등록→재조정검토)를 기존 데이터(MidtermReview·ActionItem·RebaselineRequest)로 도출, 페이지 상단 고정 — "점검→평가→검토→피드백→재조정 루틴" 가시화 ②다른 평가 페이지(eval/self·kpi)와 디자인 통일(그룹 섹션 색 #1B64DA/#029359·상태 배지·카드·PageHeader) ③`NAV_ICONS.midterm = Milestone` 누락 아이콘 추가 ④"지금 할 일" CTA·진행 표시 UX 개선. QA PASS(typecheck·build 통과, 회귀 0) | apps/web(MidtermStepper·AppShell·MidtermProgressTable·Card·eval/midterm/*), _workspace/01·04·05 | 사용자 피드백: "계획한 루틴이 안 보임·디자인 불일치·사이드바 아이콘 없음" |
| 2026-06-09 | **속도 우선 모드(옵트인) 규칙 추가** — 기본은 토큰 절약, 사용자가 "빠르게/속도 우선/병렬로" 명시 시 전환. ①병렬 팬아웃(의존성 없는 에이전트 한 메시지 동시 호출, 배리어 금지, 화면별 개별 병렬) ②컨텍스트 선적재(레퍼런스·산출물 발췌 인라인으로 Read 왕복 제거) ③중복/투기 실행(재시도 잦은 구간만 N변형 생성→판정) ④모델 상향으로 재작업 제거. 효과·구조변경 비용 순 적용, 기본은 ①②만 | orchestrator SKILL.md, CLAUDE.md | 사용자 피드백: "토큰 더 쓰더라도 속도 높이는 방법을 규칙에 넣자" |
| 2026-06-11 | **디자인 전면 재스킨 준비 — 하네스 디자인 권위 교체 (Toss→Kinetic Enterprise)** — ①루트 `DESIGN.md`를 Kinetic Enterprise 스펙으로 교체(퍼플/블루/틸 팔레트, 기본 글꼴 Pretendard[사용자 확정 — 원본 Manrope·Hanken Grotesk 대체], 8px rounded, 보라 틴트 그림자, 퍼플 사이드바) + 프로젝트 적용 노트(글꼴·등급 S~D 색 파생·데이터 밀도 보정·RBAC/hooks 유지) ②하네스 전반의 Toss/TDS 참조 갱신(orchestrator·wireframe-to-design·nextjs-frontend 스킬, product-designer·frontend-engineer 에이전트, reference-ui-screens.md) ③드리프트 해소: 06-04 폐기 선언된 `tds-design-language.md`를 실제 스텁화하고 잔존 참조 4곳 제거 — 시각 언어 SSOT를 루트 DESIGN.md로 단일화. **apps/web 코드 재스킨은 아직 미실행(다음 단계)** | DESIGN.md, CLAUDE.md, orchestrator SKILL.md, skills/wireframe-to-design·nextjs-frontend, agents/product-designer·frontend-engineer, references/tds-design-language·reference-ui-screens | 사용자: "프론트 싹 갈아엎기 — 하네스부터, DESIGN.md를 이 파일(Kinetic Enterprise)로 교체" |
| 2026-06-11 | **모델 정책 상향 → 당일 철회(원복)** — backend fable·대형 패스 opus 상향을 시도했으나 컨텍스트·토큰 소비 과다로 같은 날 원복(backend opus, 나머지 sonnet). 교훈: **속도·품질이 필요하면 모델 일괄 상향이 아니라 병렬 팬아웃(속도 우선 모드 ①)을 먼저 적용** | orchestrator SKILL.md, agents/backend-engineer.md, CLAUDE.md | 사용자: "하네스 원래대로 — 컨텍스트·토큰 너무 먹음, 병렬 팬아웃으로" |
| 2026-06-12 | **전면 디자인 통일 재스킨 실행 (속도 우선 · 9 에이전트 병렬 팬아웃)** — 기준 3탭(대시보드·내평가표 `eval/my`·KPI작성 `kpi`)을 **불가침 레퍼런스**로 디자이너가 패턴 추출(`_workspace/01_design/reskin-brief.md`) 후, 나머지 전 페이지를 9그룹(평가폼/중간점검·재조정/결과·역량/검토·이의·알림/리포트3종/조직·사용자·권한/관리핵심4/관리데이터5/인증) 병렬 재스킨 + UX 개선(빈 상태 CTA·로딩 스켈레톤·제출/위험작업 확인 모달·sticky 표 헤더·진행 표시·인라인 검증·tabular-nums·34px 수치 카드). 등급 색 S=#3f2c80/A=#0054ca/B=#4CAF50/C=#FF9800/D=#F44336 통일, Toss 잔재(#3182f6·gradeChipColor) 재스킨 범위에서 제거. QA PASS(tsc 0건·build 35라우트·기준 화면/공용 프리미티브 불가침·데이터 레이어 불변, QA가 결함 2건 직접 수정). 잔여 권고: 기준 화면 내부 #3182f6 3건(불가침이라 보존), GRADE_BADGE 상수 12파일 중복 → lib 통합 후보 | apps/web 전 페이지(기준 3탭·공용 프리미티브 제외), _workspace/00_input·01_design·04_frontend·05_qa | 사용자: "기준 3탭 빼고 싹 다 디자인 통일 + UX 개선, 에이전트 최대 병렬·토큰 무관" |
| 2026-06-15 | **멀티서비스 아키텍처로 하네스 개편 (Phase 1 — 문서·하네스 정렬, 코드 불변)** — 서비스가 늘어나는 플랫폼·바이브 코딩(AI 가독성)·외부 API 연동에 맞춰 ①신규 SSOT `architecture.md`(Turborepo+pnpm 모노레포·모듈러 모놀리식·바운디드 컨텍스트·모듈 경계=미래 절단선·DB schema 분리·수직슬라이스·**파일당 ~200줄 상한**·모듈 매니페스트 README·신규 서비스 스캐폴드 체크리스트) ②신규 `integration-adapter.md`(외부 API 소비=부패방지 어댑터·제공=키 인증/버전호환) ③`api-contract-convention.md`에 OpenAPI 발행(`@nestjs/swagger`)·orval codegen·버저닝·외부 인증 추가 ④오케스트레이터 스택표/레퍼런스표 갱신 ⑤backend·frontend 에이전트에 모듈 경계·OpenAPI codegen·파일상한 반영 ⑥api-backend·nextjs-frontend 스킬 정렬. **결정: 폴리레포 검토 후 모노레포로 확정**(1인 유지보수—원자적 커밋·단일 CI). 코드 모노레포화(pnpm workspace·turbo.json·packages 분리·Swagger 발행)는 Phase 2~3에서 점진 | references/architecture·integration-adapter·api-contract-convention, orchestrator SKILL.md, agents/backend·frontend, skills/api-backend·nextjs-frontend, CLAUDE.md | 사용자: "서비스 계속 늘어남·바이브 코딩(AI가 파일 읽기 쉽게·파일 안 길게)·외부 API 연동, 모노레포(Turborepo+pnpm)로 하네스부터 개편" |
| 2026-06-15 | **하네스 멀티에이전트 감사 + Phase 1 잔여 정렬 완결** — 5개 관점(드리프트·아키텍처 전파·커버리지 갭·품질·오케스트레이터) 병렬 감사(40건→14 우선순위)로 1차 개편이 backend·frontend·계약 라인만 정렬됐음을 발견하고 보완: ①**현행(Phase 1) vs 목표(Phase 2~3) 과도기 경계 명시**(backend·frontend 에이전트·nextjs-frontend — 없는 packages/·features/ import 환각 차단) ②스킬 호출 강제 통일(backend·release·designer의 "또는 그 절차를 따른다" 우회 제거) ③오케스트레이터에 멀티서비스 작업유형 라우팅표(외부통합·신규서비스·모노레포토대·codegen)·description 정정(다면평가 제거)·발행우선 Phase4·에러표 2행·Phase0 대상식별 추가 ④QA 라인(integration-qa F/G/H 검증영역·qa-inspector 아키텍처 게이트) ⑤릴리스 라인(release-engineer·deployment-pipeline 모노레포 turbo/pnpm/codegen 선행·모노레포 토대 소유) ⑥디자인 라인(product-designer·wireframe-to-design packages/ui 공유 전제) ⑦domain-model §4-1 바운디드 컨텍스트↔모듈↔@@schema 매핑 신설 + business-rules 컨텍스트 태그 + api-backend 모듈 정렬 ⑧tds 폐기 각주. 신규 에이전트/스킬 신설은 불필요(1인 유지보수 — 기존 역할 흡수) | agents/* 5종, skills/* 6종, references/domain-model·business-rules, CLAUDE.md | 사용자: "하네스 부족·아쉬운 점 멀티에이전트로 점검하고 보완" |
| 2026-06-15 | **하네스 재검증(3관점) — 잠복 SSOT↔코드 드리프트 핵심 오류 정정** — 회귀·교차모순·아키텍처건전성 재검증에서 **domain-model/business-rules가 평가를 "단일 부서장 평가(round=1)"·"역량 폐기"·"Position enum"으로 기술**하나 실제 코드는 다름을 발견(코드로 직접 검증): ①**다단계 부서장 평가 3단계**(round1 팀장·round2 본부장·round3 그룹대표, `combineStages` 단계가중 0.5/0.3/0.2 + 동일인 예외 ①1차=최종 100% ②2차=최종 1차70%+최종30%, `RuleSet.weightPolicy` 설정) — 하네스 전반의 "단일"·"2단계(1·2차)" 표기를 **전부 3단계로 정합**(domain-model·business-rules·CLAUDE.md·backend·api-backend·qa·integration-qa·designer·wireframe·nextjs·orchestrator·reference-ui) ②**역량평가 = 폐기 아님, 참고용 백데이터**(CompetencyQuestion/Response, 연봉·등급 0% 반영=`combineFinal` perf 1·comp 0) — domain-model 정정 + 엔티티 표 추가 ③**직책 = enum 아닌 관리형 PositionDef 레지스트리**(string·시스템 10코드·무소속 허용) 정정 ④**아키텍처 read-model 예외** 신설(모듈 간 DB 직접 조인 금지는 쓰기 경계만; results/reports 교차조회는 read model/스냅샷 허용 — 현행 results.service include와 정합) ⑤Prisma multiSchema 외래키 제약·npm→pnpm 전환 위험단계·CI 게이트(contracts#generate diff=빌드실패, release 소유)·OpenAPI 봉투 getSchemaPath+allOf 패턴·orval mutator 인증 이관 경고 추가 ⑥용어 드리프트(분리형 API→모듈러 모놀리식) 정렬. MEMORY 인덱스의 역량30% stale 훅도 정정. 검증 정합 항목(조직 4단계·등급 풀=그룹·모델 정책·Phase 번호·Pretendard)은 모순 없음 | references/domain-model·business-rules·architecture·api-contract, CLAUDE.md, agents/* , skills/* , memory/MEMORY.md | 사용자: "하네스 한번 더 — 부족·아쉬운·핵심 오류 점검" |
| 2026-06-16 | **Playwright E2E 테스트 하네스 구축 (planner/generator/healer)** — 기존 5인 개발 하네스에 Playwright Test Agents 3종 추가. ①루트 `e2e/` 전용 워크스페이스(`@growthx/e2e`) 신설 — `playwright.config.ts`(webServer 로 web:3000·api:4000 자동기동·`reuseExistingServer`, baseURL·ko-KR, projects setup→chromium), `config.ts`(공유 상수), `tests/global.setup.ts`(**인증 셋업** — `/auth/login` 직접 호출 → JWT 를 **storageState localStorage `gx.*` origin :3000** 주입; 프론트가 쿠키 아닌 localStorage 사용이라 핵심 함정), `tests/fixtures.ts`(인증된 page 진입점), `tests/seed.spec.ts`(시드 테스트=루프 진입점), `specs/`(planner 산출물), README 매니페스트 ②`npx playwright init-agents --loop=claude` 로 planner/generator/healer + `playwright-test` MCP 생성 → 루트 `.claude/agents/`(기존 5종과 공존)·루트 `.mcp.json`(`run-test-mcp-server --headless --config e2e/playwright.config.ts`)으로 이전 ③루트 `test:e2e`/`test:e2e:ui` 스크립트·pnpm-workspace 에 `e2e` 추가 ④메모리 `no-preview-verification`(프리뷰 검증 금지) → E2E 실기동 검증으로 정정 + `playwright-e2e-harness` 신규. **검증: 실기동 Docker 스택 대상 setup+seed 2 PASS(2.2s)·tsc 0건.** | e2e/*, .claude/agents/playwright-*, .mcp.json, package.json, pnpm-workspace.yaml, CLAUDE.md, memory | 사용자: "Playwright E2E 테스트 하네스 구축" |
| 2026-07-03 | **전 화면 디자인 통일 — DESIGN.md "Part 하이브리드" 승격 + 잔재 전량 제거** — ①DESIGN.md 전면 재작성(Notion 저채도 본문 폐기 → Part 클라이언트 확정안을 유일 SSOT로: 카드 10px·쿨그레이·#0257CE·사이드바 전용 퍼플·등급 5색, part-revision-brief §13 미결 해소) ②6워커 병렬 재스킨: radius-0 잔재 147건·69파일, 웜그레이·#0075DE 25건·6파일, 본문 퍼플(대시보드 타일), 로그인 장식 블롭, 컬러 섀도 전량 제거 ③검증자 적대 검수로 발견된 결함 수정: 컨트롤 rounded-lg 오적용(preset 리맵 lg=10px — 컨트롤·inset 21곳 md=8px로), 12px 카드 1건, Button danger red solid→outline, preset sm=6px 라벨 정합, grade.ts Soft 틴트를 palette.ts/DESIGN.md와 단일화. 최종: 금지 패턴 grep 0건(허용 예외 Tabs 언더라인만), tsc 0, build 35라우트 PASS, 97파일 ±450줄 | DESIGN.md, apps/web(공용·features 전반), packages/ui/tailwind-preset.cjs, lib/grade.ts, CLAUDE.md | 사용자: "페이지마다 제각각인 디자인을 Fable5 판단으로 새로 통일, 최난도부터, 검증자 서브에이전트 검수" |
| 2026-07-06 | **전 코드베이스 버그 감사 + 심각도별 수정 (Fable5 전 과정)** — 6워커 병렬 읽기전용 감사(백엔드3·프론트2·경계면1)로 ~70건 발견 후 난이도 최상단부터 수정. **Critical 5**: ①등급 계산 floor semantics — 소수 총점(95.5)·달성률(99.5%)이 등급 밴드 간극에서 D로 추락(`scoreToGrade`/`rateToGrade`/`countToGrade` min-내림차순 매칭) ②JWT 공개 폴백 키(`change-me-*`) 제거+부팅 검증(`jwt.config.ts`)+`.env` 회전 ③본인평가 로딩 전 임시저장이 실적 전량 삭제(빈 kpiScores 전송 차단) ④미제목 KPI 카드 조용히 삭제. **High**: 반려후 재제출 데드락(patch 전이), 가중치 조작(KPI 권위 weight), 그룹등급 항상 D(재정규화 `computeGroupScore`), 임의 downward 생성 차단, raw 500/Prisma 오류 마스킹, 타인 연봉 노출(users API HR·본인만), **dual ApiError 통합**(lib/api↔contracts 단일 클래스 → codegen 401 refresh 재시도+세션만료 리다이렉트, dead 에러분기 6곳 소생), 절대금액 KPI 제출불능, 반려상태 dead분기, 규칙검증 홀, 권한 자기잠금, 보상 사이클 오염, finalize 단계게이트. **Medium**: GET 부수효과 삭제, 중간점검 게이팅, 사이클 RuleSet 오염, 엑셀 멱등·트랜잭션·round3 익스포트, 업로드 크기제한. Fable5 검증자 2종(백엔드 수치 시뮬레이션·프론트/계약 적대검수) FAIL 0·회귀 0, api/web tsc 0·build 35라우트 PASS. **의도 보류 4건**(compensations 부서장 연봉·round3 예외② 오분류·재조정 총점 미재계산·lows) → [[bug-audit-deferred-items]] | apps/api(scoring·evaluations·results·auth·filters·excel·midterm·cycles·group-performance·departments·users·main), apps/web(lib/api·hooks·eval-*·kpi·admin-*·components), packages/contracts(mutator·runtime), _workspace/06_release/RELEASE.md, CLAUDE.md | 사용자: "실제 버그·깨지는 예외·사용자 노출 문제 심각도별로 찾고 수정, 최난도부터, 검증자 서브에이전트, 전 과정 Fable5" |
| 2026-07-06 | **권한별 사용자 테스트 + 확정 버그 수정 (Fable5 전 과정, 라이브 스택)** — 5개 역할(hr_admin·division_head·team_lead·employee)+RBAC 보안 감사를 실기동 Docker 스택 대상 병렬 에이전트로 실제 로그인·curl 재현 → 후보를 적대적 검증자(Fable5)로 독립 재현. **인증계층·다단계 평가 점수결합·RBAC 경계는 전부 방어 성공**(JWT 위조/수직·수평 상승 전 공격 실패), 결함은 설정·수명주기·데이터최소화에 집중. **확정 9건 수정**: ①**HIGH 권한 영구 자기잠금** — `PUT /permissions/config`로 hr `권한 부여·수정`=false 저장 시 전 hr_admin 잠금·API복구불가(직전 07-06 "자기잠금" 수정이 `PermissionsView.tsx` 프론트 전용이라 API PUT이 우회) → `mergeMatrix`가 hr `권한 부여·수정`·`시스템 설정`=true 강제 핀(저장·조회 양쪽, 오염 DB row 자가치유) ②**Med–High 퇴사/강등 후 토큰 유효(~1h)** — JWT가드가 클레임 신뢰 → 매요청 DB `isActive`·role 재확인(강등 즉시 반영, refresh도 isActive 검사) ③**Med 빈 kpiScores[] 초안 전량삭제**(백엔드 무가드, 프론트에만 가드) → `length>0` 가드 ④**Med 피평가자 미확정 하향평가 조기열람**(검토자 노트·단계등급) → getDetail 403 + 목록 점수 리댁션 ⑤**Low–Med 스테일 보상행 미정리**(등급 리셋 후) → compute 트랜잭션 고아 deleteMany 재조정 ⑥**Low 연봉변경 감사누락** → updateSalary audit.record(actor 전달) ⑦**Low /compensations 관리자 부하연봉 노출**(/users 마스킹 우회) → toDto canSeeSalary 마스킹 ⑧**Low GET /grade-pools 읽기중 삭제** → 형제 group-performance처럼 read deleteMany 제거(고아정리는 compute만) ⑨**Low 임의 feature키 수용** → mergeMatrix FEATURE_KEYS 화이트리스트. 검증자 4종 전 수정 FIXED·회귀0(평가자/HR 접근·finalize·simulation 정합 유지, 역할강등·compute 고아정리 라이브 증명). api tsc 0·이미지 재빌드·스택 healthy·cycle mid_review 원복. 잔여 후속(범위 밖): matrix `__proto__`키→500(400이어야), create/update 사용자 감사, simulation 연봉정책 결정 | apps/api(permissions/perm-config·auth/jwt-auth.guard·evaluations·compensations·users·grade-pools), CLAUDE.md | 사용자: "Fable5로 권한별 사용자 테스트·오류/버그 다 잡아·최난도부터·검증자 서브에이전트·끝나고 수정" |
| 2026-07-06 | **전면 누수 감사 2차 — 저장 초기화·결과 게이트 형제 누수 대량 수정 (Fable5, 12워커 병렬 + 검증자 2종)** — 데이터손실·상태전이·권한·경계면 4영역을 다관점 병렬 감사(~40 확정, 여러 감사관 교차확인) 후 난이도 최상단부터 파일격리 병렬 수정. **저장 초기화 계열(사용자 핵심)**: ①경영실적 임시저장이 확정(final)월 draft 강등→집계이탈(값동일 no-op+dirty만 전송) ②경영실적 매출 null→0 강제·미입력월 0행 오염(빈월 제외) ③GroupPerformance GET조회가 수동입력 덮어쓰기+orders null소거(orders 미포함·동일값 skip) ④본인평가 부분누락 임시저장이 미포함 KPI 점수 삭제(deleteMany를 전송 kpiId로 축소) ⑤**정성 KPI 메모만 저장→조용히 D 저장·복원·완료처리**(directGrade 미제공시 grade=null·score=0 미평가) ⑥KPI 서술필드 비우기 저장불가·부활(빈값=클리어 전송, undefined만 유지) ⑦부서장 종합등급 오버라이드 "해제" 미전파→확정 오염(clearOverallGrade DTO 신호) ⑧RuleSet 저장시 stageExceptionWeights 등 미노출키 삭제(weightPolicy 부분머지) ⑨일정 저장시 targetUserIds/Deptids 리셋→전임직원 팬아웃(미제공 보존) ⑩일정 잠금토글이 편집중 일정 초기화(낙관갱신) ⑪중간점검 총평단독 저장이 무조건 self_done 전이+빈리뷰(checkIns>0만 제출) ⑫KPI스냅샷 재생성 비원자(트랜잭션) ⑬역량 점수없는 문항 코멘트 소멸(grade optional·dirty보존) ⑭마감일 UTC자정→KST 09시 조기종료(end-of-day 정규화). **결과 게이트 형제 누수(직전 수정 연장)**: /compensations list·simulation·team + dashboard avgRaiseRate + evaluations/grade-distribution(cycleId 우회 포함) + 역량응답 전사노출(userId 미지정 스코프) + 미확정 하향평가 history/evidence 조기열람 — 전부 calibration/closed·hr_admin면제 규칙 통일. **프론트 게이트 드리프트**: EvaluationSummary·EvalResult·Reports·ResultDetail의 "확정/평가완료/집계완료" 오표기→"잠정" 배너·문구, ResultDetail blacklist→whitelist(draft/active 구멍·null폴스루 제거), DistFootnote "실적60%+역량40%" 오기→"실적100%(역량 참고용)". **상태전이 무결성**: 평가/KPI closed 사이클 쓰기게이트(create/patch/submit/addComment/evidence), appeals reevaluate closed 재오픈 차단·중복 차단, KPI 자기승인 차단, closed 사이클 메타(year/날짜) 변경 차단. **레이스/기타**: 보상·eval-self·kpi 훅 cancelled 가드, 총평 저장후 타이핑 롤백 dirty가드, percentile/companyAvg 집계후 전체 재계산, 이메일 편집 무시→읽기전용, 설정 알림토글 no-op→정직안내, 정성 중간점검 항상 at_risk→중립. **검증**: Fable5 적대적 검증자 2종(백엔드/프론트) — REGRESSION 0·STILL_BROKEN 1(grade-distribution cycleId 우회 → 즉시 수정) 외 전항목 CONFIRMED_FIXED. api·web tsc 0·next build 35라우트 PASS·이미지 재빌드 스택 healthy. **라이브 재현 증거**: employee/div_head mid_review에서 /compensations·grade-distribution 빈/차단·hr_admin 열람, cycleId 우회 수정 재현, RuleSet 부분PATCH 후 stageExceptionWeights 보존, 정성 KPI 메모만→grade=NULL/score=0·directGrade→A/93. **후속(범위밖)**: [[bug-audit-round2-deferred]] — excel 재임포트 덮어쓰기 3건(권위소스 판단 필요)·dashboard calibration 본인혼입·simulationTeam 음수meta·본인평가 수치 클리어 비영속 | apps/api(evaluations·compensations·dashboard·results·competency·rule-sets·cycles/schedules·snapshots·appeals·midterm·monthly-performance·group-performance·grade-pools·users), apps/web(eval-self·eval-dept-head·kpi·eval-midterm·admin-compensation·admin-cycle·admin-users·admin-settings·reports·reports-summary·eval-result·eval-result-detail 및 hooks·types), CLAUDE.md | 사용자: "누수 싹 다 잡아·저장 초기화도·완벽하게·최난도부터·행동할 정보 충분하면 행동·검증자 서브에이전트·전과정 Fable5" |
| 2026-07-07 | **권한 저장 풀림 원인 규명 + 가시성 스코프 누수 점검·수정 (Fable5, 감사 2 + 검증자 9/9 PASS)** — 사용자 신고: 권한 매트릭스/nav/가시성 저장이 풀리고, 팀장 자기팀·본부장 자기본부·그룹대표 자기그룹 규칙에 구멍 의심. **원인 규명(라이브 DB+코드)**: ①권한 매트릭스 PUT/병합/upsert는 **정상**(Node UTF-8 재현으로 team 값 보존 확인 — 최초 shell 재현의 team true→false는 Git Bash 한글 heredoc 인코딩 손상 착시) ②실제 "풀림" 원인 = **seed 재실행이 저장된 권한·nav를 DEFAULT로 파괴**(seedPermissionConfig upsert의 update 브랜치) → **existing 있으면 보존(create-only)**. permission_config row 0개였던 것도 재시드 흔적 ③mergeMatrix가 미일치 키(인코딩 손상·오타)를 **무경고 200으로 기본값 치환**하던 것 → console.warn 진단 로그(저장은 유지) ④**NAV 키 드리프트** — 프론트 midterm·eval-summary 백엔드 미등록, 백엔드 eval 유령 키 → NAV_KEYS 프론트와 정합(matrix식 화이트리스트 정렬 시 메뉴 숨김 저장 풀리던 지뢰 제거). **가시성 스코프**: 코어(visibleDeptIds)는 형제 본부/그룹 배제 정확·개인 교차누수 0건 확인. 실제 위반 1건 = **`GET /evaluations/grade-distribution`이 본부장(scope=division)에게 그룹 전체(형제 본부 포함) 부서별 등급분포 노출**(형제 results.distribution은 올바름) → effectiveGroupId 수집 후 **visibleDeptIds 교집합**으로 좁힘(본부장→자기 본부, 팀장→자기 팀, 그룹대표→그룹 전체 유지, hr 면제). 검증: Fable5 검증자 9/9 PASS·회귀 0(형제 배제 access.util 대조), api tsc 0·이미지 재빌드 healthy. **라이브 증거**: Node UTF-8 PUT 후 team 권한 저장 유지·nav에 midterm/eval-summary 반영·테스트 row 정리. **후속 관찰(범위 밖)**: mergeNav 내부 키 무검증(구 eval 키 잔존 무해), grade-distribution 하위단계 평가자의 상위 미확정 열람(정책 판단), role↔scope 우선순위 모듈별 비대칭(misconfig 전제) → [[bug-audit-round2-deferred]] 계열 | apps/api(prisma/seed·evaluations.service·permissions/perm-config.constants), CLAUDE.md | 사용자: "권한 매트릭스·사이드바·가시성 저장 풀림·스코프(팀장 자기팀/본부장 자기본부/그룹대표 자기그룹) 구멍 점검" |
| 2026-07-07 | **KPI 카테고리 게이트·절대금액 등급·직급 관리 3종 단순화 (Fable5, 병렬 2워커 + 검증자 8/8 PASS)** — 사용자 결정: 실통제는 상급자 승인이므로 설정 게이트 불필요. 근거 데이터: KPI 439개 전부 서술형(qualitative)·절대금액 사용 0건·targetValue 보유 0건. ①**카테고리 직책자 게이트 제거** — kpis.service의 assertCategoryAllowedForUser+assertCategoryWritable(role 게이트)+categoryPolicy 주입 제거, KpiWriteView의 allowed 필터 제거 → 전 사용자 5개 카테고리 작성 가능(카테고리=분류 라벨). kpi-category-policy 모듈·CategoryPolicyMatrix는 소비처 0 잔존 코드로 유지 ②**절대금액 등급 생성 경로 제거** — KpiWriteView/KpiDraftCard 토글·목표금액 입력 제거(draftToPayload 항상 qualitative), RulesView+RuleSetEditor의 revenueGradeScale 편집 섹션(~140줄) 제거. 백엔드 스코어링 경로·schema·eval 화면 분기는 호환 유지(데이터 0건 dormant), weightPolicy 스프레드 머지로 DB 키 보존 ③**직급 관리 축소** — PositionsTab 모달=직급명 1개(코드·정렬 자동, DTO 필수 필드는 백엔드 기본값 상수 비노출 전송), 목록=직급명·코드·액션만. 신규 등록 초기 role 파생(백그라운드)은 유지. 검증: Fable5 적대 검증자 8/8 PASS(등급 S~D 기준 편집 온전·기존 수정분 4종 보존·엑셀 임포트도 qualitative 상수라 충돌 없음 확인), api·web tsc 0·build PASS·이미지 재빌드 healthy. **라이브 증거**: employee가 매출액·수주 카테고리 KPI 생성 성공(이전 422 CATEGORY_NOT_ALLOWED)·테스트 데이터 정리 | apps/api(kpis.service·kpis.module), apps/web(features/kpi 4파일·admin-rules/RulesView·components/RuleSetEditor·admin-users/PositionsTab), CLAUDE.md | 사용자: "직책자만 매출·수주 작성 게이트 불필요(승인이 통제)·절대금액 등급 토글 불필요·직급 관리는 직급명만 — 그렇게 해" |
| 2026-07-07 | **평가자 배정 B-1 전환 — 권한(role)과 평가 조직 분리, 명시 부서장 단일 기준 (Fable5 + 검증자)** — 문제: 평가자 식별이 role(team_lead/division_head)·직책 자동 추론 혼용이라 hr_admin 권한 승격 시 부서장이 배정 후보에서 빠짐(정재훈·김테스트는 명시 지정으로만 우연히 커버, 연구팀은 이미 1차 평가자 구멍). 사용자 확정: "권한만 열린 것, 평가는 사용자 관리의 조직 구조대로" + 계층 미지정/본인=단계 비우고 상위가 평가(본부장=대표만)·그룹대표 무평가. ①`access.util.ts deptHeadUserId`를 **Department.headUserId 명시 지정 단일 기준**으로(role·position 추론 폐기, 본인·비활성 제외) — 소비처 5곳(평가 생성권한·자동배정·중간점검·재조정·보완조치) 자동 전파 ②백필 `prisma/backfill-dept-heads.ts`(`backfill:dept-heads`, --dry-run·멱등) — 전환 직전 추론 결과를 영속화, 40개 부서 중 39개 지정(연구팀=팀장 없음 유지) ③검증자(Fable5)가 잡은 규칙 위반 수정: **부그룹장 지정 그룹의 그룹대표가 부그룹장에게 round1을 받는 기형 체인**(박창영←홍종필, round3 없음) → 그룹대표(headUserId 본인)는 resolve 조기 반환 {}로 무평가 확정 ④재배정(reset) 적용 후 라이브 증명: 일반직원 1·2·3차 정상(진희선), 팀장→1차 본부장(민명기·김테스트), 본부장→최종만(이현우), 그룹 직속 임원→최종만(박성현), 연구팀(그룹 직속)→그룹장만(고수민), 그룹대표 0건(홍두화·박창영), 부그룹장 설계 유지(임하나 1차=홍종필 — 기존 확정 설계). api tsc 0·이미지 재빌드·스택 healthy. **잔여(잠복, 후속)**: 부서장 지정 시 소속/role 미검증(employee-role 부서장은 배정돼도 @Roles 게이트에 화면 접근 불가·코멘트 필수 우회) — 운영 가이드: 부서장 지정 시 팀장/본부장 이상 role 함께 부여 | apps/api(common/access/access.util·prisma/backfill-dept-heads 신규·package.json), memory(dept-head-by-role 개정), CLAUDE.md | 사용자: "hr_admin은 권한만, 평가는 조직 구조대로 — B-1 진행, 계층 비면 상위가 평가, 그룹대표 무평가" |
| 2026-07-07 | **주기(사이클) 선택 아키텍처 개편 — 전역 공유 → 작업 고정 + 페이지-로컬 URL (Fable5, 병렬 2워커 + 검증자)** — 문제: 주기 선택이 사용자 구분 없는 localStorage(`eval.selectedCycleId`) 전역 상태라 ①같은 브라우저의 다른 계정으로 선택이 번지고 ②13개 화면이 한 상태를 공유해 "어디서 바꿨는지 모르는" 혼란. 사용자 선택 방안 3 적용: ①`useCurrentCycle` 읽기 전용화 — setSelectedId·localStorage 제거, 항상 진행 주기(active→mid_review/calibration→첫 항목) 반환, 레거시 키 마운트 시 정리 ②신규 `useCycleParam`(hooks) — `?cycleId=` URL 쿼리 기반 페이지-로컬 선택(router.replace·기존 파라미터 보존·새로고침/딥링크 유지), useCurrentCycle+setSelectedId 드롭인 대체 ③**작업 화면 진행 주기 고정**: MidtermView 셀렉터 제거(주기명 라벨만; eval-self·kpi·eval-dept-head는 원래 고정) ④**조회 5**(내 평가표·평가결과 상세·리포트·평가자정리·역량) + **관리 3**(평가 운영·경영실적·역량 문항) 화면은 useCycleParam + Suspense 래핑(useSearchParams CSR bailout 방지, ResultDetailView 패턴) ⑤CompensationView는 기존 자체 로컬 셀렉터 유지. 검증자(Fable5) 적대 검수: 전 항목 PASS·경미 1건(내 평가표→평가결과 상세 링크 cycleId 소실 → 즉시 수정), 생성 직후 표시 시나리오는 useAsync stale-유지+로딩 스켈레톤 조합으로 안전 판정. web tsc 0·build 35라우트 PASS·웹 이미지 재빌드 | apps/web(hooks/useCurrentCycle·useCycleParam 신규, features/eval-my·eval-result-detail·reports·reports-summary·competency-eval·admin-cycle·admin-monthly-performance·admin-competency-items·eval-midterm), CLAUDE.md | 사용자: "주기 토글이 A계정에서 바꾸면 B계정도 바뀜·여러 화면에 있어 헷갈림 — 방안3(작업 고정+조회만 페이지-로컬)로 진행" |
| 2026-07-06 | **결과 공개 게이트 + 내 평가표 일정 정합 (Fable5 직접)** — 평가 미완료(mid_review, 3차 not_started)인데 잔존 집계 행(과거 최종단계 compute 후 사이클 원복) 때문에 내 평가표에 등급 노출·일정 불일치. ①**결과 공개 게이트(백엔드 results)**: 비HR 결과 열람은 calibration·closed 에서만, **본인 결과는 closed 이후에만**("캘리브레이션 끝나면 공개" 제품 문구와 정합) — getDetail/export=404 미공개, list/summaryTable=행수준 where(cycle.status+본인행), distribution=빈 분포. hr_admin 면제(집계·조정 업무) ②**dashboard.summary 동일 게이트** — me.finalGrade/hasResult·등급분포가 mid_review 에 잔존 행 노출하던 것 차단 ③**내 평가표(MyEvaluationView)**: 결과 fetch 자체를 cycle closed 게이트(요약 카드·상세평가표·스테퍼 등급 자동 비노출), 완료 단계 배지 '대기'→'완료' ④**일정 정합**: 스테퍼가 전 단계에 final_review 마감(12.31)을 폴백 도배+완료 단계에 마감일을 완료일처럼 표시하던 것 제거 — 운영 일정에 동일 phase 저장 시에만 단계 마감 표시, 카드 상단에 "최종평가 기간(평가 운영 일정 기준) 12.01~12.31" 명시(운영 저장값 그대로). 라이브 검증: employee 계정 mid_review 전 엔드포인트 404/빈 응답·closed(2025) 본인 결과 정상·hr_admin 불변, api·web tsc 0·build PASS·이미지 재빌드 스택 healthy | apps/api(results.service·dashboard.service), apps/web(features/eval-my/MyEvaluationView), CLAUDE.md | 사용자: "평가 안 끝났는데 결과 요약·상세평가표·진행 현황에 등급 노출, 평가 일정이 평가 운영 저장값과 불일치 — 수정" |
| 2026-07-06 | **그룹 부그룹장(부대표) 중간 평가 단계 신설 (Fable5 직접, 하네스 미사용)** — 발단: 홍종필 부대표(친환경기술그룹, scope=group)의 하향평가 목록에 부하 0명·박창영(그룹장) 1건만 표시. 원인은 코드가 아니라 배정 구조: 그룹장=박창영이라 51명 전원의 round3가 박창영에게 배정. 사용자 확정 구조(직원=팀장→본부장→그룹대표 / **팀장=본부장→부대표→그룹대표 / 본부장=부대표→그룹대표**)를 일반화해 구현: ①`Department.deputyHeadUserId`(group 전용, 마이그레이션 `20260706090000`) ②`resolveDownwardEvaluators` 재작성 — **round1·2 = 피평가자에서 가까운 상급 장 순서 압축**(팀장·본부장·부그룹장 사슬 중 2명), round3 = 항상 그룹장. 부그룹장은 사슬 끝이라 일반 직원 평가엔 자연 미관여, 장(팀장·본부장)·그룹직속 인원 평가에만 들어옴. **부수 의미 변경: 팀장의 본부장 평가가 round2→round1**(가중 재정규화 60/40→71/29, "팀장부터는 1차=본부장" 사양 그대로) ③departments PATCH `deputyHeadUserId`(활성 검증·group 외 400)+org-chart deputy 노출 ④조직 구조 보드에 부그룹장 지정/해제 버튼·배지(그룹 전용) ⑤고정 단계 라벨("1차(팀장)" 등) 중립화 — MyEvaluationView·SummaryRowExpand·EvalReport·ResultDetailView·DeptHeadEvalView·EvaluatorFlow(1·2차는 피평가자별로 다름). 데이터: 친환경기술그룹 deputy=홍종필 지정 + auto-assign reset(초기화 265·신규 283·보존 4 — finalized 3·in_progress 1 보존). 검증(라이브): 직원/팀장/본부장/부대표/그룹장/그룹직속/본부없는팀 체인 전부 사양 일치, 홍종필 평가자 17건(1차 6·2차 10·최종 1), 타 그룹 부작용 없음, api·web tsc 0·이미지 재빌드 healthy. 점수결합은 라운드 번호 가중(0.5/0.3/0.2)+재정규화라 무변경 호환 | schema.prisma+migration, apps/api(access.util·departments·org-chart·evaluations 주석), apps/web(types·useDepartments·OrgStructureBoard·AdminUsersView·라벨 6파일), CLAUDE.md | 사용자: "친환경기술그룹만 예외 — 팀장은 1차 본부장·2차 홍종필 부대표, 본부장은 1차 부대표·최종 그룹대표. 하네스 쓰지 말고 Fable5로" |
| 2026-07-07 | **KPI 엑셀 임포트 파서 결함 6종 수정 — 실양식 47개 전수 재현·검증 (Fable5 직접)** — 사용자 신고 "파싱이 깨지는 경우 많음"을 `26년도 KPI 친환경기술그룹` 실파일 47개로 전수 재현(45개 이상). 확정 결함·수정: ①**퍼센트 서식 숫자 등급기준 깨짐(최다)** — 엑셀 "100%"/"0%"/"30%"가 raw 1/0/0.3이라 등급기준 B가 "1"로 저장 → `cellText()`(numFmt '%' 인지) 신설, 등급기준·CSF·제목·목표·측정방식 추출 전환 + num() "10%" 텍스트 처리 ②**핵심전략 빈칸 행 통째 유실** — CS1본부 12명 전원 협업 KPI(가중치 10%) silent drop → 분류 사다리(표준→CSF/KPI명 폴백→스템 폴백, 폴백은 실데이터 행에만) ③**자유 서술 핵심전략 전량 유실** — 이두환 5행 전부 drop → 스템 자동분류(+`확인해 주세요` warning) & 미인식+실데이터 행은 valid=false 기본분류로 미리보기 노출(조용히 버리지 않음) ④**한 시트 표 2개 이어붙임** — CS2_1팀 4명 가중치 합 200%·20행 → 두 번째 표 헤더('핵심전략' 재등장)에서 중단+미수집 표 라벨 경고 ⑤**개인 시트 대신 범용 템플릿 시트 선택** — 한상윤("KPI작성(한상윤)"+"개인별 KPI작성" 공존, 범용 4열 가중치 오파싱 합 130%) → 시트 선택을 KPI 헤더 탐지+가중치 열 최소 우선으로 ⑥**가중치 열 미탐지/`-` 행** — 오재용(헤더 "5년차 미만") 가중치 전부 null → 측정방식↔S열 사이 구조 폴백, `-`만 있는 행=역할 변형 "해당 없음"으로 스킵+경고. **preview 응답에 `warnings[]` 추가**(OpenAPI 재발행+orval 재생성) + 프론트 AdminKpiImportView에 파싱 경고/오류 배너·행별 message 표시(기존엔 경고 비표시라 조용히 깨짐). 검증: 컴파일된 실서비스로 47개 전수 — **전 파일 가중치 합 100%·데이터 유실 0**(잔여 INVALID 2건=원본 결함의 의도된 노출, "0" 등급기준=원본 실값), api·web tsc 0·build 35라우트, 라이브 스택 preview 엔드포인트 재검증 | apps/api(excel.service·kpi-import-response.dto), packages/contracts(재생성), apps/web(AdminKpiImportView), CLAUDE.md | 사용자: "26년도 KPI 친환경기술그룹 실파일 살펴보고 파싱 깨지는 것 해결" |
| 2026-07-07 | **KPI 검토 순차 결재선 도입 — 팀장 승인 후 본부장·그룹대표 단계 결재 (Fable5 직접, 사용자 선택: 순차 결재선)** — 기존 KPI 검토는 가시범위 내 아무 부서장 1명이 승인→확정(프론트가 approve+confirm 연쇄 호출, approved 후 반려 전이 자체가 없음). 사용자 요구 "팀장이 승인해도 본부장이 반려, 본부장이 승인해도 대표가 반려" → **평가 체인과 동일한 결재선**(resolveDownwardEvaluators: 1차 팀장→2차 본부장→최종 그룹대표, 부그룹장 압축·headUserId 명시지정)으로 재설계. ①스키마: `Kpi.approvalStage`(완료 단계 수)+`approvalTrail`(이력 JSON), 마이그레이션 `20260707120000`(기존 approved 행 stage=1 백필) ②전이표: submitted→[approved,confirmed,draft]·approved→[approved(중간),confirmed,draft]·confirmed→[draft](HR 전용) ③서비스: approve=자기 단계 차례에만(+hr_admin 대리, 위반 시 단계 안내 403), 마지막 단계 승인=confirmed·중간=approved+다음 결재자 알림(kpi_approval_pending)·확정 시 작성자 알림(kpi_confirmed); reject=결재선 구성원 누구나 **하위 승인 후에도** 가능(stage·trail 리셋, confirmed 되돌림은 hr_admin만); confirm 엔드포인트=approve 별칭(호환); submit/재제출 시 stage 리셋; 신규 `GET /kpis/approval-chain/:userId` ④프론트(kpi-review): 결재선 배너(1차→…→최종, 본인 강조)·행별 "결재 n/m" 칩·내 차례에만 승인 버튼("n차 승인"/"최종 승인")·반려/수정요청은 체인 구성원 상시·결재 이력 표시, approve+confirm 연쇄 제거; KPI 상태 라벨 approved='결재중'(eval-self·KpiWriteView 문구 정합) ⑤**중간점검은 변경 불필요 확인** — 백엔드 assertReviewerAuth 가 이미 round1~3 전원 허용 + confirmed→revision_requested(승인 후 반송) 존재, 화면(ReviewSplitPanel)도 승인 완료 상태 재조정 요청 노출, 상위 장 목록도 evaluatorId 기준이라 커버. 검증: 라이브 스택 시나리오(test1 체인: test@팀장→이현우 본부장→홍두화 대표) — 결재선 3단계 조회·건너뛰기 승인 403·체인 밖 403·본인 403·**1차 승인 후 본부장 반려→draft**·재제출 후 3단 순차 승인→confirmed·이력 3건·확정 후 본부장 반려 403/HR 되돌림, api·web tsc 0·build 35라우트·contracts 재생성(127 paths)·이미지 재빌드 healthy. 유의: 본인평가는 confirmed KPI 만 사용하므로 전 결재 완료가 평가 전제(기존 정합 유지), 기존 approved 데이터는 2차 대기로 편입 | schema.prisma+migration, apps/api(transitions·kpis.service·kpis.controller·kpi-response.dto), packages/contracts(재생성), apps/web(lib/types·lib/ui·features/kpi-review 4파일·eval-self·kpi 문구), CLAUDE.md | 사용자: "팀장이 승인해도 본부장이 반려, 본부장이 승인해도 대표가 반려 — KPI검토·중간점검 로직 수정. 순차 결재선 선택" |
| 2026-07-07 | **KPI 임포트 중복 적재 사고 수정 — "임하나 140%/14개" 신고 (Fable5 직접)** — 원인은 파서가 아니라 **적재 수명주기**: importKpi/commitKpi 멱등 교체가 draft 만 삭제해, 이전 적재분을 제출(submitted)한 뒤 재임포트→재제출하면 동일 KPI 가 두 벌 누적(임하나 14개/200%·이대길 21개/300%·이두환 10개/200% — 07-06·07-07 배치 중복, 파서 출력 자체는 7개/100% 정상). 게다가 submitImportedKpi 가중치 검증이 **이번에 제출할 draft 만 합산**해 기존 제출분과 합계 200%를 통과시킴. 수정: ①importKpi·commitKpi 교체 범위를 draft+**submitted**(미결재)로 확대 — 결재 진행(approved)/확정(confirmed)본만 보존 + "교체되지 않았어요, 반려로 되돌린 뒤 재적재" 경고 ②submitImportedKpi 검증을 **잔존 KPI(비draft) 포함 전체 합산**으로(중복 시 제출 차단 안전망) ③데이터 정리: 피해 3인의 구 배치 26행 삭제(종속 레코드 0 확인 후, 최신 배치 보존) → 3인 모두 7·5·7개/100% 복구. 검증(라이브): 임하나 재임포트→기존 submitted 7개 교체(deleted=7)→제출→재임포트→재제출 반복해도 7개/100% 유지, api tsc 0·이미지 재빌드 healthy | apps/api(excel.service — importKpi·commitKpi·submitImportedKpi), 라이브 DB 정리, CLAUDE.md | 사용자: "임하나 가중치 100인데 140, 항목 14개 — 파싱 제대로 봐야" |
| 2026-07-08 | **다단계 평가 예외 판정 구멍 2건 수정 (Fable5 직접, 수치 시뮬레이션 8/8 PASS)** — 사용자가 PPT 규칙(1차50/2차30/최종20, 예외①1차=최종→100%, 예외②2차=최종→70/30)을 재확인해 검증 요청 → 가중치·예외 로직·seed·라이브 RuleSet 정합 확인 후 구멍 2건 수정. ①**미확정 오분류(구 보류항목#2 해소)** — 예외 판정용 평가자 식별이 finalized 평가만 봐서 "최종 배정됐지만 미확정" 시점 집계가 예외②(70/30)를 오발동 → **배정된 평가 전체(상태 무관)** 기준으로 변경(점수는 여전히 확정분만, 미확정 단계는 재정규화 62.5/37.5로 수렴 후 재집계 시 50/30/20) ②**겸직 예외② 미인식** — 그룹장이 2차 자리(본부장·부그룹장 계층) 겸직 시 배정 중복제거로 round2 부재 → 71.4/28.6 오적용 → `resolveDownwardEvaluators`에 `finalAlsoSecond` 플래그 신설(겸직 미제거 자리 목록의 2번째=그룹장 판별), aggregate 가 가상 round2 평가자로 주입해 기존 예외② 로직(70/30) 발동. 팀장 피평가(1차 본부장+최종 그룹장, 겸직 아님) 71.4/28.6 재정규화·구조적 2단 체인 예외②·체인1명 예외①은 불변(회귀 0, 시뮬레이션 8케이스). 같은 날 UI 3건: KPI 검토 카드 칩 컬럼 고정폭(목표/측정 라인 정렬), KPI 작성 목표수립 가이드 패널 제거, 가중치 라벨 줄바꿈 수정 | apps/api(common/access/access.util·results/results.service), apps/web(kpi-review/KpiReviewSubComponents·kpi/KpiWriteView·KpiDraftCard, KpiGoalGuidePanel 삭제), memory(bug-audit-deferred-items #2 해소), CLAUDE.md | 사용자: "평가 프로세스(50/30/20+예외①②) 잘 반영됐는지 구멍 확인 → 둘 다 수정" |
| 2026-07-07 | **중간점검도 KPI와 동일한 순차 확인 결재로 전환 (Fable5 직접, 사용자 선택)** — 기존 중간점검은 체인(round1~3) 누구나 병렬 확인/반송 가능(단계 없음) + 화면도 앞 단계 미완 건에 승인/재조정 버튼 노출(KPI 검토에서 지적된 것과 동일 혼란). ①스키마: `MidtermReview.reviewStage`+`reviewTrail`(마이그레이션 `20260707130000`, 기존 confirmed 행 stage=1 백필) ②confirm=자기 단계 차례에만(+hr 대리, 위반 시 단계 안내 403) — 중간 단계는 status **self_done 유지**+stage/이력만 누적, **마지막 단계에서만 confirmed**(confirmedAt) ③sendBack=진행 중엔 체인 구성원(하위 확인 후에도 — 팀장 확인 후 본부장 반송), **confirmed 되돌림은 hr_admin 전용**, 반송·재제출 시 stage/이력 리셋 ④화면: MemberDetail 이 `GET /kpis/approval-chain`(동일 원천) 로 체인 로드 → ReviewSplitPanel 판정·승인은 **내 차례에만**(아니면 "n차 확인 대기 · 이름"/"내 확인 완료 n/m" 배너+이력), 승인 버튼 라벨 "n차 승인/최종 승인", 헤더 배지 "확인 n/m 진행중", 확정 후 재조정 요청 버튼은 HR에게만(비HR은 안내 문구) ⑤toDto·MidtermReviewDto에 reviewStage/reviewTrail 추가(OpenAPI·contracts 재생성). 검증(라이브, test1 체인): 자가점검 제출→본부장 건너뛰기 확인 403→1차 확인(self_done/stage1)→**본부장 반송(1차 확인 후)**→재제출→1·2·최종 순차 확인→confirmed/이력 3건→확정 후 본부장 반송 403/HR 되돌림, api·web tsc 0·build 35라우트·이미지 재빌드 healthy | schema.prisma+migration, apps/api(midterm-reviews.service·midterm-response.dto·transitions 주석), packages/contracts(재생성), apps/web(lib/types·eval-midterm api/hooks/MemberDetail/ReviewSplitPanel), CLAUDE.md | 사용자: "중간점검도 똑같은 로직으로 반영해놨지? → KPI와 동일한 순차 결재 선택" |
| 2026-07-08 | **전 화면 데스크탑 UI/UX 개편 — 사용성·가독성 감사 후 대량 수정 (Fable5 전 과정: 감사 7 + 수정 워커 8 + 검증자 2)** — 28개 feature 전 화면을 7그룹 병렬 감사(P0 9·P1 ~40·P2 ~50 수집) 후 최난도부터 파일 소유권 격리 8워커 병렬 수정(~125건). **P0 해소**: ①`DataTable` stickyHeader 구조적 미동작(th sticky+스크롤 컨테이너 단일화 `bare` prop+기본 maxHeight+키보드 행 진입) ②페이지별 브라우저 탭 제목(metadata template+27라우트, 클라이언트 페이지는 layout 신설) ③KPI 부분 반려 재제출 불능(draft만 합산하던 가중치 게이트→`combined` 합산, 백엔드 검증과 정합) ④KPI 검토 그리드 1366px 붕괴(중복 측정방식 열 제거) ⑤중간점검 부서장 카드가 정량 자가점검 수치 미표시+거짓 빈 문구 ⑥부분 제출/재제출이 순차 확인 결재를 무경고 리셋(확인 모달+제출 n/m 병기) ⑦경영실적 최종저장 데드락(!dirty 게이트 제거+확정 확인 모달, 임시저장은 dirty 게이트 강화) ⑧조직 보드 부서장 표시 role 폴백 제거(B-1 headUserId 단일 기준 정합+미지정 warning 배지·건강카드) ⑨일정 알림 채널 no-op 체크박스 정직화. **전역**: Modal 85vh+내부 스크롤·본문 대비, 에러 토스트 8s, EvaluationActionPanel `sticky` prop(긴 폼 4곳 하단 고정), `--gx-header-h` 변수로 페이지 sticky 오프셋 통일, 사이드바 접힘 localStorage, body 전역 tnum 제거, `STAGE_LABEL` 단일 소스(단계 라벨 4종 표기 통일), StatusBadge `domain='kpi'`(submitted='결재 대기' 렌더), appeals 상태 라벨 통일(접수/최종완료). **주요 P1**: 부서장 평가 임시저장 배선(+종합등급 오버라이드 포함·stale detail 가드·코멘트 dirty 유지)+제출 후 다음 대상 자동 이동+상태 필터, 미저장 이탈 beforeunload 가드 5화면, 중간점검 탭 상시 마운트(전환 유실 제거)·의견 프리필 분리(재귀속 방지)·진척 스트립, KPI 일괄 승인·[수정요청] 파싱·확정 KPI 상시 표시, 원가 증감 색 반전, 임포트 검토 필요 파일 자동 커밋 스킵+경고 유지, 권한 dirty sticky 저장 바+본인 강등 차단, 규칙 저장 dirty+확인 모달+등급 갭 검증, ScheduleEditor 이중 표 통합(즉시 적용/저장 시 반영 구분), 이의제기 진입 안내·3단계 스테퍼, 대시보드 합성 피드→실제 알림·할 일 최상단·waiting 배지, 로그인 아이디 저장 실동작, **평가 결과표 엑셀 내보내기가 존재하지 않는 엔드포인트 호출(전원 404)→실존 경로+hr 게이트 정정**, 500명 하드캡 절단 배지, 명부 임포트(RosterImportPanel) 진입점 배선, 감사 로그 user/PermissionConfig 필터. 검증: Fable5 적대 검증자 2종 — V1 평가 플로우 12항목·V2 전역/관리 15항목 전수 판정, 발견 결함 전부 즉시 수정(임시저장 dirty 부분 해제·stale 레이스, '결재 대기' 데드 라벨, 미정의 warning/danger/info 200·300 셰이드→프리셋 보강, DataTable 키 버블 가드). web tsc 0·build 35라우트 PASS. **후속(범위 밖)**: 월별 확정 상태 표시·감사 로그 서버 검색·대상 요약 라벨(백엔드 필요), 표 3벌 DataTable 수렴, sticky 첫 열 행 틴트, Stepper blur-커밋 첫 클릭, HR 일괄 승인 확정 문구 | packages/ui/tailwind-preset.cjs, apps/web(components 15·features 26개 전 그룹·app metadata 29·lib/ui·permConfig·globals.css), CLAUDE.md | 사용자: "전체 페이지 UIUX 수정 — 사용 편의·가독성 최대화(데스크탑), 스킬 미사용·Fable5 판단·최난도부터·검증자 서브에이전트" |
