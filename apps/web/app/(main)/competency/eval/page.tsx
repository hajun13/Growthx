import { CompetencyEvalView } from '@/features/competency-eval/ui/CompetencyEvalView';

export const metadata = { title: '역량평가' };

export default function CompetencyEvalPage() {
  return <CompetencyEvalView />;
}
