# 프론트가 호출할 API 베이스 URL.
# 비워두면 same-origin('/api/v1')로 호출(리버스 프록시/rewrite 사용 시).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# (선택) next.config rewrites 로 /api/* 를 백엔드로 프록시할 때 사용.
# 설정 시 NEXT_PUBLIC_API_BASE_URL 은 비워두고 same-origin 으로 호출.
# API_PROXY_TARGET=http://api:4000
